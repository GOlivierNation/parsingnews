{% extends 'base.html.twig' %}

{% block title %}Failed messages{% endblock %}

{% block body %}
<h1>Manage messages</h1>
<table>
    <thead>
        <tr>
            <th>id</th>
            <th>Namespace</th>
            <th>Newsletter</th>
            <th>User</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>

        {% for message in messages %}
            <tr>
                <td>{{message.id}}</td>
                <td>{{message.title}}</td>
                <td>{{message.message.newsId}}</td>
                <td>{{message.message.userId}}</td>
                <td>
                    <a href="{{ path('newsletters_failed_resend', {id: message.id}) }}">Resend</a>
                    <a href="{{ path('newsletters_failed_delete', {id: message.id}) }}">Delete</a>
                </td>
            </tr>
        {% endfor %}
    
    </tbody>
</table>

{% endblock %}
